<div class="container">
  <div id='booking-title'>
    <h1>My bookings</h1>
    <%# <h4><%=# pluralize @bookings.size, 'booking'</h4> %>
  </div>
  <div class="btn-user">
    <%= link_to "Sitter", bookings_path(type: "sitter"), class: "btn-user-status #{@profile == "sitter" ? "active" : nil}" %>
    <%= link_to "Owner", bookings_path, class: "btn-user-status #{@profile == "sitter" ? "active" : nil}" %>
  </div>
    <%# <% if @booking.profile_sitter.id == current_user.id %>
    <%# stocker booking avec .profile_sitter.id == current_user.id dans un array %>
    <%# demander a un TA %>

      <%# -------- When sitter -------- %>
      <div class="card-container">
        <% @bookings.each do |booking| %>
          <div class="cards-category m-4">
            <% if booking.user == current_user %>
              <% if @bookings.any? %>
                <div class="inner-card">
                  <h4><%= booking.profile_sitter.user.first_name %></h4>
                </div>
                <div class="inner-card">
                  <p><%= booking.start_date %></p>
                  <p><%= booking.end_date %></p>
                </div>
                <%= image_tag 'paris-map.jpg', height: 180, width: 238, id: "map" %>
                <div class="inner-card">
                  <p><%= booking.status %></p>
                </div>
              <% else %>
                <div class="card-check-sitters">
                  <h4>Nobody booked you yet! 😿</h4>
                  <div class="btn-check-sitters">
                    <%# <p><%= link_to 'Change your availibilities!', !!!!, class: "btn btn-ghost" %></p> %>
                  </div>
                </div>
              <% end %>
            <% else %>
              <% if @bookings.any? %>
                <div class="inner-card">
                <h4><%= booking.user.first_name %></h4>
                </div>
                <div class="inner-card">
                  <p><%= booking.start_date %></p>
                  <p><%= booking.end_date %></p>
                </div>
                <%= image_tag 'paris-map.jpg', height: 180, width: 238, id: "map" %>
                <div class="inner-card">
                  <p><%= booking.status %></p>
                </div>
                <div class="d-flex justify-content-around mb-3" id='accept'>
                  <%= link_to accepts_booking_path(booking), method: :patch, class: "btn-planning small" %>
                  <i class="fas fa-check"></i>
                  <%= link_to "Decline", decline_booking_path(booking), method: :patch, class: "btn-planning small" %>
                </div>
              <% else %>
                <div class="card-check-sitters">
                  <h4>You don't have any bookings yet!</h4>
                  <div class="btn-check-sitters">
                    <p><%= link_to 'Check sitters available around you!', profile_sitters_path, class: "btn btn-ghost" %></p>
                  </div>
                </div>
              <% end %>
            <% end %>
          </div>
        <% end %>
      </div>
<%# <% end %>

<%# si pas de booking, rajouter une phrase qui dis 'you haven't any booking yet' %>
<%# triple bouton, quand tu cliques sur 'no anwser' ou 'decline' ou 'accepted'
 il change le status avec la string en question --> javascript %>
 <%# quand 'decline' la card se supprime de tous les bookings %>
<%# quand il clique sur 'accept' le booking est rajouté à ceux du pet owner %>
