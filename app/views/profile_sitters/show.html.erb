<div class="row py-5 px-4">
  <div class="col-md-5 mx-auto w-80">
<div class="card-trip">
  <div class="card-trip-infos">
    <div class="d-flex justify-content-between">
      <img src="<%= cl_image_path @profile_sitter.user.photo.key %>" class="avatar-bordered mb-4" />
      <div class="p-3 mb-2">
        <h1><%= @profile_sitter.user.first_name %> <%= @profile_sitter.user.last_name %></h1>
        <p><%= @profile_sitter.address %></p>
      </div>
    </div>
  </div>
      <h2 class="mx-3">Available from <%= @profile_sitter.start_date.strftime("%B %d") %> to <%= @profile_sitter.end_date.strftime("%B %d") %></h2>
    <p class="m-3"><%= @profile_sitter.description %></p>
  <div class="d-flex justify-content-center">
    <p><%= link_to "Book", new_profile_sitter_booking_path(@profile_sitter), class: "btn btn-booking m-2 col text-center" %><p>
    <p><%= link_to "Go back", profile_sitters_path, class: "btn btn-booking m-2 col text-center" %><p>
  </div>
  <div class="d-flex justify-content-center">
  </div>
</div>
    <%# <%= @profile_sitter.user.photo.key %>
  </div>
</div>

<div id="booking-form-div"
     data-bookings="<%= @bookings_dates %>">
</div>
